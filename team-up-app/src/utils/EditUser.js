import firebase from './Firebase.js';

const database = firebase.database();

// Modifies an existing user in the database
// Default method stub, modify method as needed
/*
export function editUser(userUID, description, email, name) {
    database.ref('/users/')
            .child(userUID)
            .update({
                description: description,
    });
}
*/
